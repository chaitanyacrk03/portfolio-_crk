import { JumpingDirective } from './jumping.directive';

describe('JumpingDirective', () => {
  it('should create an instance', () => {
    const directive = new JumpingDirective();
    expect(directive).toBeTruthy();
  });
});
